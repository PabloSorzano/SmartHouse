<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ipn.com.mx.smarthome">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_smart"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_smart"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Principal" />
        <activity android:name=".controles.ControlCasa"></activity>
        <activity android:name=".agregar.AgregarCasa"></activity>
        <activity android:name=".agregar.AgregarCuarto"></activity>
        <activity android:name=".agregar.AgregarDispositivo"></activity>
    </application>

</manifest>